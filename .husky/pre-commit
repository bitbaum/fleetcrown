#!/usr/bin/env sh
# Run quick correctness checks on every commit. Smoke runs in pre-push
# (when the local server is up) — see .husky/pre-push.

set -e

echo "→ tsc --noEmit"
npx tsc --noEmit

echo "→ eslint src/"
npx eslint src/
