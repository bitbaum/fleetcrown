/**
 * SSOT for the private-zone PIN digit cap. Client inputs (maxLength) and the
 * server validator (/api/auth/pin/setup) must agree, so both import from here.
 */
export const PIN_MAX_DIGITS = 12;
