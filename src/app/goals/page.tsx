import { Target } from "lucide-react";
import { PlaceholderView } from "@/components/shell/PlaceholderView";

export default function GoalsPage() {
  return <PlaceholderView icon={Target} title="Goals" />;
}
